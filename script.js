const radius = 1;
const latSteps = 20;
const longSteps = 20;

let vertices = [];

for (let latitude = 0; latitude <= latSteps; latitude++) {
  let theta = (latitude * Math.PI) / latSteps;
  for (let longitude = 0; longitude <= longSteps; longitude++) {
    let phi = (longitude * 2 * Math.PI) / longSteps;
  }

  let x = radius * Math;
}
